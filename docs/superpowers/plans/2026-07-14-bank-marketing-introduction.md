# Bank Marketing Introduction Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and verify a one-page formal Word introduction for the bank marketing classification experiment.

**Architecture:** A small Python builder creates the DOCX from verified experiment facts. The packaged document renderer converts the result to PNG for visual inspection, followed by a structural assertion pass.

**Tech Stack:** Python 3, python-docx, LibreOffice document rendering.

## Global Constraints

- Output exactly one final DOCX at `D:\UniFiles\Python_ML\OtherFiles\银行营销分类实验简介.docx`.
- Use the `standard_business_brief` preset and a simplified `memo_masthead` title block.
- Keep the document to one page and do not modify the experiment code, dataset, report, or existing defense script.
- Use verified values: 45,211 records; 5,289 positive samples; 36,168 training samples; 9,043 test samples; random forest without duration metrics 0.8599/0.4194/0.5142/0.4620.

---

### Task 1: Build the introduction document

**Files:**
- Create: `D:\UniFiles\Python_ML\.codex_tmp\build_experiment_introduction.py`
- Create: `D:\UniFiles\Python_ML\OtherFiles\银行营销分类实验简介.docx`

**Interfaces:**
- Consumes: verified experiment facts from `D:\UniFiles\Python_ML\CodeFile\bank_marketing_experiment.py` and the existing defense materials.
- Produces: a one-page DOCX with title, metadata, three short sections, and a highlighted conclusion.

- [x] **Step 1: Write the DOCX builder**

Create a deterministic `python-docx` script that applies explicit page, font, spacing, color, and paragraph-style values.

- [x] **Step 2: Run the builder**

Run: bundled Python on `D:\UniFiles\Python_ML\.codex_tmp\build_experiment_introduction.py`

Expected: the final DOCX exists and is larger than 10 KB.

### Task 2: Render and verify the result

**Files:**
- Inspect: `D:\UniFiles\Python_ML\OtherFiles\银行营销分类实验简介.docx`
- Create for QA only: `D:\UniFiles\Python_ML\.codex_tmp\experiment_introduction_render\page-1.png`

**Interfaces:**
- Consumes: the DOCX generated by Task 1.
- Produces: one rendered page and a structural verification result.

- [x] **Step 1: Render the DOCX**

Run the packaged `render_docx.py` with the bundled Python runtime.

Expected: exactly one page PNG is produced.

- [x] **Step 2: Inspect the rendered page**

Open `page-1.png` at original detail and verify that text, spacing, headings, and footer are clean.

- [x] **Step 3: Run structural assertions**

Verify that the DOCX contains the correct title, student identity, record count, four metrics, no placeholder markers, and one section.

Expected: all assertions pass.
